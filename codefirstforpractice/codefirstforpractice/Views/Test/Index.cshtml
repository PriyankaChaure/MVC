@model IEnumerable<codefirstforpractice.Models.player>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.sportseries.soprtseriesname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.team.teamname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.playername)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.scoreplayer)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.sportseries.soprtseriesname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.team.teamname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.playername)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.scoreplayer)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.playerid }) |
                @Html.ActionLink("Details", "Details", new { id=item.playerid }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.playerid })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
